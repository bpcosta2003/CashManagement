# Copie este arquivo para `.env.local` e preencha com os valores do seu
# projeto Supabase (Project Settings → API).
#
# .env.local é ignorado pelo git (NÃO COMMITAR).
#
# Estes valores são públicos por design — vão para dentro do bundle JS.
# A segurança vem das políticas Row-Level Security definidas em
# supabase/schema.sql.

VITE_SUPABASE_URL=https://SEU-PROJETO.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...
